extends ../pug/html.pug

block variables
  - var pagetitle = 'Access Forbidden';
  - var footerHidden = true;

block content
  h1 #{pagetitle}
  +error-page('403')
  p Um.... looks like you don't have access to view this particular page. Looks like the issue is on my side.

