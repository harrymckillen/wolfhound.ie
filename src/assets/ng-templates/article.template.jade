div.a-column.width-1-of-12.article-preview(ng-repeat="article in articles")
  div.postinfo
    span Issue \#{{article.issue}}
    span.middot &middot;
    span(ng-bind="article.date | date:'LLLL dd, yyyy'")
  a(ng-href="{{article.link}}")
    h2 {{article.title}}
    p {{article.intro}}
    span.ghost-button.read-article.inline-block(ng-href="{{article.link}}") Read article
